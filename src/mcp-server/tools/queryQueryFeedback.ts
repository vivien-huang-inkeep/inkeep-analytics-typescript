/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queryQueryFeedback } from "../../funcs/queryQueryFeedback.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.QueryFeedbackRequestBody$inboundSchema,
};

export const tool$queryQueryFeedback: ToolDefinition<typeof args> = {
  name: "query-query-feedback",
  description: `Query Feedback`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await queryQueryFeedback(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
