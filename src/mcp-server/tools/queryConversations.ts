/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queryConversations } from "../../funcs/queryConversations.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.QueryConversationsRequestBody$inboundSchema,
};

export const tool$queryConversations: ToolDefinition<typeof args> = {
  name: "query-conversations",
  description: `Query Conversations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await queryConversations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
