/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queryQuerySemanticThreads } from "../../funcs/queryQuerySemanticThreads.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.QuerySemanticThreadsRequestBody$inboundSchema,
};

export const tool$queryQuerySemanticThreads: ToolDefinition<typeof args> = {
  name: "query-query-semantic-threads",
  description: `Query Semantic Threads`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await queryQuerySemanticThreads(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
