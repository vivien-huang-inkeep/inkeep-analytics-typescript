/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { queryQueryEvents } from "../../funcs/queryQueryEvents.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.QueryEventsRequestBody$inboundSchema,
};

export const tool$queryQueryEvents: ToolDefinition<typeof args> = {
  name: "query-query-events",
  description: `Query Events`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await queryQueryEvents(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
